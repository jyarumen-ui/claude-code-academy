# フロントエンド（Vite）環境変数。VITE_ プレフィックス必須。
# ⚠️ ここに置けるのは公開前提の値のみ（anonキー等）。シークレットは置かない。

# バックエンドのURL（設定するとAI生成・メディア・分析・鍵秘匿プロキシが有効化）
VITE_API_BASE=

# Supabase（anon / publishable キーのみ。RLSで保護すること）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
