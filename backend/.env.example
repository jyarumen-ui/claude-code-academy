# バックエンド環境変数（本番では各ホスティングのSecretsに設定）
# ⚠️ 実値はコミットしない。鍵はローテーション後の新しい値を入れること。

# 投稿文のAI生成（/api/social/generate）
OPENAI_API_KEY=

# Typefully 予約・メディア・分析（/api/social/*）
TYPEFULLY_API_KEY=
# 既定のソーシャルセットID（令和の大殺界 = 310432）。リクエストで上書き可
TYPEFULLY_SOCIAL_SET=310432

# サーバーポート（任意）
PORT=3001
